import s from './LinkedProducts.module.scss';

const LinkedProducts = ({linkedProducts}) => {
  return (
    <div className={s.linkedProducts}>
      LinkedProducts
    </div>
  );
};

export default LinkedProducts;